interface:
  display_name: "Release"
  short_description: "Release an already prepared and reviewed Rust or Python packa..."
  default_prompt: "Use $release to follow the repository workflow."
policy:
  allow_implicit_invocation: false
